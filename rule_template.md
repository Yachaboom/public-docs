# {{ sdk_name }} {{ version }} 참조 문서 활용 가이드 작성 요청

## 🎯 목표
{{ sdk_name }} {{ version }}의 **LLM 활용 최적화 가이드**를 작성해주세요.
이 가이드는 LLM이 생성된 참조 문서를 효과적으로 활용하여 정확하고 실용적인 {{ sdk_name }} 코드를 생성할 수 있도록 도와야 합니다.

## 📁 문서 구조 정보
### 생성된 문서 파일들
1. **main-documentation.md** - 완전한 참조 문서 (가장 상세하고 포괄적)
2. **llms.txt** - 빠른 참조용 인덱스 (문서 구조와 핵심 개념 요약)
3. **llms-full.txt** - 통합 전체 내용 (모든 내용을 하나의 파일로 통합)
4. **{{ sdk_name | lower | replace(' ', '-') }}-reference-rule.md** - 이 활용 가이드

## 🧠 LLM 활용 전략 가이드

### 질문 유형별 문서 활용 전략

#### 1. 빠른 정보 확인 (Simple Query)
```
질문 예시: 
- "{{ sdk_name }}에서 {{ sdk_type | get_common_operation }} 사용법이 뭐야?"
- "주요 클래스 생성자 매개변수가 어떻게 되나?"
- "기본 설정 옵션이 뭐야?"

접근법: llms.txt → main-documentation.md 해당 섹션
```

#### 2. 구현 예제 필요 (Implementation Example)
```
질문 예시:
- "고급 기능을 구현하려면 어떻게 해야 해?"
- "실제 통합 예제 완전한 코드를 보여줘"
- "에러 처리 포함한 실제 사용 예제 알려줘"

접근법: main-documentation.md의 "실전 예제" 섹션
```

#### 3. 전체적인 이해 필요 (Conceptual Understanding)
```
질문 예시:
- "{{ sdk_name }} 아키텍처를 전반적으로 설명해줘"
- "주요 개념과 컴포넌트 관계가 뭐야?"
- "다른 {{ sdk_type }} 라이브러리와 비교해서 특징이 뭐야?"

접근법: llms-full.txt 또는 main-documentation.md의 "기본 개념" 섹션
```

#### 4. 복잡한 통합 질문 (Complex Integration)
```
질문 예시:
- "프로덕션 환경에서 {{ sdk_name }}을 안정적으로 운영하려면?"
- "{{ language }}와 {{ sdk_name }}을 통합해서 완전한 애플리케이션 구현하려면?"
- "성능 최적화하면서 확장 가능한 아키텍처 설계하려면?"

접근법: llms-full.txt에서 여러 섹션 통합 참조
```

## 🎯 {{ sdk_name }} 특성별 접근 전략

### {{ sdk_type | title }} 중심 개발 패턴
{% if sdk_type == 'web-framework' -%}
```
우선 참조 섹션:
- 기본 설정 → main-documentation.md의 "설치 및 설정"
- 라우팅 → "라우팅 시스템" 섹션
- 미들웨어 → "미들웨어/인터셉터" 섹션
- 데이터베이스 연동 → "실전 예제" 섹션
```
{% elif sdk_type == 'database' or sdk_type == 'orm' %}
```
우선 참조 섹션:
- 연결 설정 → main-documentation.md의 "연결 및 설정"
- 모델 정의 → "스키마 정의와 모델링" 섹션
- 쿼리 작성 → "쿼리 패턴" 섹션
- 성능 최적화 → "성능 최적화" 섹션
```
{% else %}
```
우선 참조 섹션:
- 기본 사용법 → main-documentation.md의 "시작하기"
- 핵심 API → "핵심 API 참조" 섹션
- 고급 기능 → "고급 기능" 섹션
- 실전 활용 → "실전 예제" 섹션
```
{% endif %}

### {{ language }} 언어 특화 고려사항
{% if language == 'Python' -%}
- **타입 힌트**: 모든 함수에 완전한 타입 정보 포함
- **비동기 처리**: async/await 패턴 적극 활용
- **패키지 관리**: pip/poetry/pipenv 설치 가이드
- **가상환경**: venv/conda 설정 방법
{% elif language == 'JavaScript' %}
- **모듈 시스템**: ES6 import/export 사용
- **비동기 처리**: Promise/async-await 패턴
- **패키지 관리**: npm/yarn 설치 가이드
- **타입 안전성**: TypeScript 사용 권장
{% elif language == 'TypeScript' %}
- **타입 시스템**: 완전한 타입 정의 포함
- **컴파일 설정**: tsconfig.json 최적화
- **인터페이스**: 명확한 타입 계약 정의
- **제네릭**: 재사용 가능한 타입 패턴
{% else %}
- **타입 시스템**: {{ language }}의 타입 시스템 활용
- **메모리 관리**: 언어별 메모리 관리 패턴
- **에러 처리**: {{ language }} 표준 예외 처리
- **패키지 관리**: {{ language }} 생태계 도구 활용
{% endif %}

## 📝 답변 작성 가이드라인

### ✅ 반드시 해야 할 것

#### 1. 정확한 문서 기반 답변
```
- 관련 섹션과 예제를 구체적으로 참조
- {{ sdk_name }} {{ version }}의 정확한 API 사용
- 최신 버전 기준 정보 제공
- 공식 문서 패턴 따르기
```

#### 2. 실행 가능한 코드 제공
```{{ language.lower() }}
{% if language == 'Python' %}
# 필요한 imports 모두 포함
from {{ sdk_name | lower }} import [필요한_클래스들]
import logging

# 에러 처리 포함
try:
    # 핵심 구현
    result = [주요_작업]()
    print(f"성공: {result}")
except Exception as e:
    logging.error(f"오류: {e}")
    raise
{% elif language == 'JavaScript' %}
// 필요한 imports 모두 포함
const { [필요한_함수들] } = require('{{ sdk_name | lower }}');

// 에러 처리 포함
try {
    // 핵심 구현
    const result = await [주요_작업]();
    console.log('성공:', result);
} catch (error) {
    console.error('오류:', error.message);
    throw error;
}
{% elif language == 'TypeScript' %}
// 필요한 imports와 타입 정의 포함
import { [필요한_타입들] } from '{{ sdk_name | lower }}';

// 타입 안전 구현
const result: [반환타입] = await [주요_작업]();
{% else %}
// {{ language }} 코드 예제
// 필요한 imports와 에러 처리 포함
{% endif %}
```

#### 3. 맥락과 배경 설명
```
- 왜 그렇게 해야 하는지 설명
- {{ sdk_name }}의 특성과 장점 강조
- 다른 방법과의 차이점 설명
- 사용 시기와 적절한 상황 안내
```

#### 4. 실전 고려사항
```
- 성능 최적화 팁
- 보안 고려사항
- 에러 처리 패턴
- 테스팅 전략
- 프로덕션 배포 가이드
```

### ❌ 절대 하지 말 것

#### 1. 추측 및 불확실한 정보
```
- 문서에 없는 내용은 명확히 "문서에서 다루지 않음" 표시
- 이전 버전 정보와 혼동 금지
- 확인되지 않은 API나 기능 제공 금지
```

#### 2. 불완전한 예제
```
- 코드 예제는 최대한 완전한 형태로 제공
- import 누락이나 실행 불가능한 스니펫 지양
- 설정 없는 예제 금지
- 에러 처리 없는 프로덕션 코드 제공 금지
```

#### 3. 버전 혼동
```
- {{ sdk_name }} {{ version }}의 정보만 기준
- 다른 버전과의 혼동 방지
- 정확한 API 명칭 사용
```

## 📋 답변 템플릿

### 기본 질문 처리 템플릿
```
## 핵심 답변
[직접적인 답변 + 실행 가능한 코드 예제]

## 상세 설명
[개념 설명 + 동작 원리]

## 실전 고려사항
[에러 처리 + 성능 + 보안 팁]

## 관련 자료
[추가 참조 섹션 + 연관 기능 안내]
```

### 복잡한 구현 질문 템플릿
```
## 전체 아키텍처 개요
[시스템 구조 + 컴포넌트 관계]

## 단계별 구현
### 1단계: [기본 설정]
[코드 + 설명]

### 2단계: [핵심 기능 구현]
[코드 + 설명]

### 3단계: [고급 기능 추가]
[코드 + 설명]

## 테스트 방법
[테스트 코드 + 검증 방법]

## 프로덕션 고려사항
[배포 + 모니터링 + 최적화]

## 문제 해결 가이드
[흔한 오류 + 해결 방법]
```

### 개념 설명 템플릿
```
## 핵심 개념
[기본 정의 + 핵심 원리]

## 동작 원리
[내부 구조 + 처리 과정]

## 사용 사례
[언제 사용하는지 + 적절한 상황]

## 코드 예제
[실제 구현 + 주석 설명]

## 관련 개념
[연결되는 다른 개념들]

## 베스트 프랙티스
[권장 사항 + 주의사항]
```

## ✅ 품질 검증 체크리스트

답변 완성 후 다음을 확인하세요:

### 내용 정확성
- [ ] 문서의 정확한 정보를 기반으로 작성했는가?
- [ ] {{ sdk_name }} {{ version }}의 최신 정보를 반영했는가?
- [ ] 공식 API 명칭과 패턴을 정확히 사용했는가?

### 코드 품질
- [ ] 제공된 코드가 실제로 실행 가능한가?
- [ ] 필요한 imports가 모두 포함되었는가?
- [ ] 에러 처리와 예외 상황을 고려했는가?
- [ ] {{ language }} 언어의 베스트 프랙티스를 따랐는가?

### 설명 품질
- [ ] 초보자도 이해할 수 있게 설명했는가?
- [ ] 단계별 접근법을 제공했는가?
- [ ] 왜 그렇게 해야 하는지 배경을 설명했는가?

### 실무 적용성
- [ ] 실전에서 유용한 조언을 포함했는가?
- [ ] 성능 고려사항을 언급했는가?
- [ ] 보안 및 안정성을 고려했는가?
- [ ] 프로덕션 환경 고려사항을 포함했는가?

### 완성도
- [ ] 관련된 다른 문서 섹션을 적절히 안내했는가?
- [ ] 추가 학습 자료를 제공했는가?
- [ ] 문제 해결 방법을 포함했는가?

이 가이드를 따라 {{ sdk_name }} {{ version }}을 사용한 정확하고 실용적인 코드 생성 및 문제 해결을 진행하세요.

---
**문서 메타데이터:**
- 생성일: {{ current_date }}
- SDK: {{ sdk_name }} {{ version }}
- 언어: {{ language }}
- 유형: {{ sdk_type }}
- 형식: LLM 활용 가이드 v1.0